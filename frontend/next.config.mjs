/** @type {import('next').NextConfig} */
const nextConfig = {
      images: {
        domains: ['ik.imagekit.io','img.icons8.com','solar-project-tracker-bucket.s3.ap-south-1.amazonaws.com'],
  },
};

export default nextConfig;
